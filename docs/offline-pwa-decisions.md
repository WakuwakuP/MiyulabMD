# PWA・オフライン閲覧：意思決定と検証の記録

機能の現在の契約は [仕様書](offline-pwa.md)、実装候補は
[レビュー資料](../review-artifacts/README.md) を参照する。
本書は選択肢・採用理由・結果・見直しを残す台帳であり、記載された方針が
すべて実装済みであることを意味しない。

## 運用ルール

ユーザーの最新指示により、実装上の問題は原則として推奨案を選んで進める。
以前の「問題発見時に停止して選択を待つ」という進め方を置き換える。
選択が後で不適切と判明した場合の手戻りも許容されている。

- 判断ごとに、背景、選択肢、選んだ案と理由、影響・制約、検証、状態を記録する。
- 方針を見直すときは古い判断を消さず、何が分かったかと後継の判断を追記する。
- サブエージェントの判断も候補内の `decisions.md` に残し、親が本書へ集約する。
- 方針選択前の状態と、選択・実装・検証の区切りをローカルコミットに残す。
- WIP、RED テスト、未採用候補を含むチェックポイントは、その旨を明示する。
- ユーザー不在時に署名が失敗するため、エージェントの今後のcommitには
  `--no-gpg-sign`を指定する。ユーザー・リポジトリ全体の署名設定は変更しない。
- コミットの記録は履歴の強制書き換えや外部公開の許可ではない。破壊的操作、
  ワークスペース外への影響、外部送信など、別途確認が必要な操作は確認する。
- 実装完了時に、採用方針、棄却・見直し、検証結果、残る制約を整理して報告する。

## チェックポイント

| コミット | 内容 | 検証・注意 |
| --- | --- | --- |
| `0a436ebf12bdd20c92d63dd231e258ae188d88b2` | 今回の未コミット作業を保存する前のベース | 旧オフライン実装の撤去後 |
| `9d7f11fad90a38d788d95d884faefa0502bdac29` | 採用済み基盤、仕様、未実装分のテスト、候補専用ランナー、過去レビュー資料を保存 | **WIP**。Web unit 117件と型チェックを親が再確認。拒否・利用停止・実画面のテストはライブ実装では未完了 |
| `0f960ecbf7bc697791b1e9f6175a90b07c175ddf` | 意思決定台帳と推奨案で進める運用を記録 | v8候補のレビュー開始前。ライブ実装は前チェックポイントと同じ |
| `e7df645b7322184a07683584ecb44b610ce8d438` | v8候補の正本、27件通過の親検証、独立レビューの不採用理由、D22の選択を保存 | ライブ実装には未採用。ここから書き込み終端の修正を開始 |
| `03c4d3738a34406359a0e430a774d639352104ac` | ユーザー停止とnative IDBの未確定／complete境界の回帰テスト、D22の再現結果 | v8で2件RED、後継候補へ29件の検証を要求 |
| `b2cd66ca6c89d03e11b1c4f90f9b525f3e9f0e5b` | エージェントcommitの署名をコマンド単位で無効化する方針を記録 | 署名設定そのものは変更していない |
| `267250bb15cbe6334d52ef65365f877d051a9f80` | 完全bytesを回収したv9、親の再検証、D26のREDテストと判断を保存 | 29件成功・追加1件失敗。ライブ実装は未変更 |

最初のチェックポイントには、ユーザーが作業開始前に行った
Biome `^2.5.12` → `^2.5.13` 更新も、その後の Playwright 導入と混在した
lockfile とともに現状保存として含めた。この依存更新をエージェントによる
新しい製品判断とは扱わない。

本書自身の変更点は `git log --oneline -- docs/offline-pwa-decisions.md` で追跡する。
後続の判断には直前のチェックポイントを記載する。自動で過去へ戻したり、
共有履歴をリセットしたりはしない。

## 既に合意・検討した判断の整理

以下は会話と仕様から確認できる判断の要約である。以前の短い「A」「OK」の返答に
新しい意味を推測して加えず、確定した仕様と実際の検証結果を記録する。
過去の候補の検証件数は、現在のライブ実装全体の完成度とは区別する。

| ID | 背景・選択肢 | 採用方針と理由 | 状態・制約 |
| --- | --- | --- | --- |
| D01 | オフライン編集も同時に扱うか、閲覧に限定するか | フォルダ探索・ノート閲覧を先行。更新キューや編集同期を持ち込まない | 合意済み。実画面への接続は未完了 |
| D02 | 全件保存の完了を管理する／閲覧済みだけ保存する／全体を先読みする | マイドライブ全体を best-effort で prefetch。キャッシュがなければ閲覧不可。完全保存を保証しない | 合意済み。prefetch 未実装 |
| D03 | IndexedDB だけで保存するか、OPFS を併用するか | メタデータは IndexedDB、本文等は OPFS、起動資産は Cache Storage | 本文・メタデータの基盤を採用済み。画像・起動資産は後続 |
| D04 | オフライン画面を別 URL に分けるか、既存画面を再利用するか | 同じ URL・画面の枠組みを使い、キャッシュ結果を preview-only にする | 合意済み。表示と更新 API の両方で制御する必要あり |
| D05 | 既存の Service Worker 撤去処理を維持するか | 直前の問題に対する撤去処理は置き換えてよい。本番へ直接デプロイして検証可能 | ユーザー承認済み。まだ新 PWA shell の実装・デプロイはしていない |
| D06 | モックだけで検証するか、実ブラウザを導入するか | Playwright Test で実 IndexedDB／OPFS を検証。テストを先に作り、GPT-5.6-luna が実装、親がレビュー | 導入済み。Vite 検証は本番 Service Worker 検証の代用ではない |
| D07 | 共有 Playwright ブラウザキャッシュか、worktree 専用か | 専用キャッシュ＋自動 GC 無効。共有ブラウザに影響させない | 導入済み。初期の共有 GC 問題を受けて見直し |
| D08 | フォルダの `null` と文字列 `"root"` を同じキーにするか | `[userId, folderId]` の構造化キーで区別。番兵文字列との衝突をなくす | 採用済み、実ブラウザの回帰テストあり |
| D09 | ローカルの前回ユーザーを認証済みと扱うか | 認証済み viewer と cached viewer を分離。保存済み ID だけで認証状態を作らない | 基盤採用済み。AppShell 接続は後続 |
| D10 | 取得処理全体の例外を通信失敗にするか | fetch／本文転送の通信失敗だけ分類。JSON不正・意図的中止・想定外エラーは隠さない | 採用済み、API read tests あり |
| D11 | 特定の HTTP コードだけを保持するか | 全非成功 HTTP 応答を一律に保持。本文不明なら statusText を使い、上位が fallback 判断 | 採用済み、400～599 の回帰テストあり |
| D12 | 中止時点で保存失敗と即決するか、トランザクション終端を待つか | complete／abort の実際の終端を保存の事実とする。セッション破棄による表示抑止とは分ける | 採用済み。確定済み本文の誤削除を防ぐ |
| D13 | 応答時のグローバル viewer を使うか、開始時に固定するか | 読み取りセッションごとに viewer を固定。本文・取得元・cachedAt を一つの結果にまとめる | 採用済み。旧グローバル案はユーザー間混入リスクで棄却 |
| D14 | 認証結果だけで編集可否を決めるか、実際の取得元も見るか | 認証済みでも cache 由来なら readonly。操作時点の状態を共通 gate が判定 | gate 基盤は採用済み。実 API／UI 接続は未完了 |
| D15 | fallback ごとに返却するか、共通の返却境界を持つか | 全非同期処理後の最終境界で有効性を確認。確認後は await せず返す | 基盤採用済み。キャッシュ読み取り中の取消テストあり |
| D16 | 403／404時に物理削除だけ行うか、論理的拒否も保存するか | 拒否を先に永続化し、物理削除は再試行可能にする | 方針合意済み、実装候補レビュー中。詳細 R01 |
| D17 | 拒否前の遅い200応答を採用するか | 要求の開始時に順序を捕捉し、拒否前の処理による復活を防ぐ。拒否後の新しい200は復帰可能 | 方針合意済み、詳細 R03 |
| D18 | ストレージ障害時に対象ユーザーのみ停止するか、全ユーザーを止めるか | 対象ユーザーの全キャッシュ操作を停止し、他ユーザーと正常なオンライン表示を維持 | 方針合意済み、詳細 R02・R04 |
| D19 | 候補をコピーしてからライブ実装を復元するか | 候補ファイルを直接編集・検証し、レビュー後だけ親が反映する | 新方式導入済み、詳細 R05 |
| D20 | 問題ごとに選択待ちするか、推奨案で進めるか | 原則推奨案を選択し、全判断をMarkdownとコミットへ記録。手戻りを許容 | 最新のユーザー指示。今回から適用 |

## R01：削除失敗で権限拒否が消える問題

- **背景**：API が403を返しても、IndexedDB の削除が失敗すると後の通信失敗で
  同じ本文が再表示された。親が実 Chromium で再現した。
- **選択肢**：A＝論理的な拒否記録と物理削除を分離、B＝ユーザー全体の利用停止。
- **選択**：A。通常の権限拒否では、そのノートだけを非表示にできるため。
- **制約**：拒否記録自身の永続化が失敗した場合は、ユーザー単位の実行中キャッシュを
  安全側へ停止する。すべての書き込みが拒否される場合の再起動後までの保証はできない。
- **検証**：`note-read-denial.spec.ts`、`note-denial-failure.spec.ts`。
- **結果**：方針維持。単純に削除失敗を握りつぶす候補は棄却。R02・R04で停止処理を補強。

## R02：DBを開けないと拒否処理を飛ばす問題

- **背景**：拒否応答後に DB open が失敗すると、optional cache の `null` として扱われ、
  警告も利用停止も行われなかった。ストレージ復旧後に本文が復活した。
- **選択肢**：A＝拒否入口をDB接続から独立させる、B＝その画面のキャッシュ全体を止める。
- **選択**：A。ユーザー分離と正常なオンライン閲覧を維持する。
- **必要条件**：対象ユーザーとノートを接続前に把握する。既存・新規ハンドルに停止を適用し、
  I/Oが再び成功しただけでは自動再開しない。HTTP拒否結果と `cacheWarning` を返す。
- **検証**：`note-denial-failure.spec.ts` の DB-open failure。
- **結果**：方針維持。開始時だけの停止チェックでは不足し、R04へ続く。

## R03：遅い成功応答が新しい拒否を取り消す問題

- **背景**：先に開始したAの200応答を遅延させ、Bの403完了後にAを完了させると、
  本文保存・禁止解除でキャッシュが復活した。親が実 Chromium で確認した。
- **選択肢**：A＝ユーザー・ノート単位で拒否前後の要求を区別、
  B＝一度拒否したノートは手動クリアまで自動復帰しない。
- **選択**：A。古い要求を無効にしつつ、権限再付与後の新しい取得で自然に復帰できる。
- **必要条件**：セッション作成時でなく各要求の開始時を基準とする。
  保存・禁止解除・ネットワーク／キャッシュ結果の最終返却に一貫して適用する。
  自分の拒否処理が世代を進めたことだけで、HTTP拒否結果まで隠さない。
- **検証**：`note-denial-ordering.spec.ts` の遅い200と進行中キャッシュ読み取り。
- **結果**：最終ガードを取得直後に置く案は不十分。全awaitの後、成功結果の返却直前へ修正。
  それでもユーザー全体の停止は別ノートへ伝わらず、R04で共通化する。

## R04：ユーザー停止が進行中処理・フォルダ・一覧へ伝わらない問題

- **背景**：v6候補は親のブラウザテスト25件を通過したが、独立レビューで次の不足が判明。
  - 利用停止前に本文読み取りを開始すると、停止後に古い本文を返し得る。
  - 別ノートの拒否記録保存失敗によるユーザー全体の停止を、ノート単位の世代では検出できない。
  - フォルダ読み書き・一覧保存に停止チェックがなく、一覧読み取りにも終端チェックがない。
  - 保存や禁止解除が開始時の判定を通過済みの場合、停止との競合を保護できない。
- **選択肢**：A＝ユーザー単位の共通ライフサイクルを全キャッシュ操作へ適用、
  B＝その画面の全ユーザーのオフラインキャッシュを停止。
- **選択**：A。他ユーザーへの影響を避け、決定済みのユーザー分離を維持する。
- **必要条件**：開始時と終端の判定、未確定トランザクションの取消、
  本文・フォルダ・一覧・禁止解除の一貫した管理。確定済みコミットを失敗扱いに
  変えたり、参照済みファイルを削除したりしない。正常なオンライン表示は維持。
- **検証**：`user-cache-suspension.spec.ts` の open／commit failure 2ケースを親が追加。
  旧候補に対して両方REDを確認。候補専用ランナーの既定対象は計27件。
- **結果**：実装中。v7は27件通過の報告があったが、候補保存に失敗したため採用不可。
  新方式の `user-cache-suspension-v8` で改めて実装・検証する。

## R05：テスト済みコードの受け渡しを失う問題

- **背景**：実装・コピー・復元の間に、候補の消失、原本復元の不一致、
  baselineを候補として保存する事故が繰り返された。v7でもテスト済み候補が残らなかった。
- **選択肢**：A＝候補専用ファイル自体を直接編集・検証、
  B＝未採用コードをライブ作業ツリーへ残し、親がレビュー・復元。
- **選択**：A。レビュー前のライブ反映を避けながら、危険なコピー・復元工程をなくす。
- **実装**：`apps/web/scripts/check-offline-candidate.mjs`。
  ブラウザは候補をViteで差し替える。TypeScript7の旧compiler APIがないため、
  型チェックは候補と同じbytesを検証専用ツリーへ一方向に配置してnative CLIで実行する。
  候補やライブソースへ書き戻さない。Biomeは候補を直接検査する。
- **検証**：候補の型チェック・Biome成功、候補にだけ入れた型エラーの検出、
  旧候補の停止テスト2件がREDになること、検証前後の元ソース・候補不変を確認。
- **制約**：テスト前後のハッシュと結果を記録し、検証後にコードを変えたら再検証する。
  旧資料には不一致があり得るため、過去の「passed」を新候補の証拠に流用しない。
- **状態**：導入済み。チェックポイント `9d7f11f` に保存。v8へ適用中。

## D21：v8のユーザーライフサイクル管理候補

- **状態**：候補内で選択後、独立レビューで不採用。D22へ見直し。
- **選択前チェックポイント**：`0f960ec`。
- **背景**：R04で決めたユーザー全体の停止を、進行中の処理にも適用する必要がある。
- **候補が検討した案**：操作開始時だけの停止チェック、ユーザーごとの寿命世代を
  完了時にも確認する方式、すべてのハンドルを停止する方式。
- **候補の選択・理由**：ユーザー単位の寿命世代を導入し、全キャッシュ種別の
  読み書きの終端で検査する。通常のオンライン表示とキャッシュ停止を切り分けるため。
- **未確認事項**：終端チェックだけで未確定の書き込みを本当に止められるか、
  確定したトランザクションを誤って失敗扱いにしないかは独立レビューで確認する。
  不足があれば、未確定トランザクションの中止などを組み合わせる方針へ見直す。
- **検証**：親も候補専用ランナーで27件のブラウザテスト、候補の型チェック・Biomeを確認。
  対象ハッシュ・正確なコマンドは
  [v8親レビュー記録](../review-artifacts/user-cache-suspension-v8/parent-review.md) に保存。
- **判断の原記録**：[候補のdecisions.md](../review-artifacts/user-cache-suspension-v8/decisions.md)。

## D22：未確定操作の取消と確定済みデータの保護

- **状態**：推奨案を選択、回帰テスト追加・実装へ進む。
- **選択前チェックポイント**：`0f960ec`。v8候補・レビュー結果・本判断を`e7df645`に保存。
- **背景**：D21のepoch終端チェックだけでは書き込みの発生自体を防げず、
  commit後の検査失敗が参照中の本文削除につながる。禁止解除にも同じ停止管理が不足。
  直接の本文読み取りと一覧の最終返却にも判定漏れがある。
- **選択肢A**：ユーザー寿命にひもづく未確定操作の取消と、実トランザクションの
  complete／abort判定を共通化。完了済みcommitのファイルは削除しない。
  キャッシュの返却直前にはユーザー寿命・必要な拒否世代を確認する。
- **選択肢B**：全キャッシュ操作をユーザー単位のキューへ直列化する。
  順序は追いやすいが、遅い読み取りの後ろで拒否処理が待つ問題と、操作間の遅延を増やす。
- **選択肢C**：完了後のepochチェックだけを増やす。
  表示の抑止には使えても、既に行われた書き込み・禁止解除や誤cleanupは防げない。
- **採用**：A。保存の事実と表示可否を分け、不要な直列化を避けながら既存の
  commit-terminalルールを全キャッシュ操作へ適用できるため。
- **影響・制約**：オンラインの正常応答はキャッシュ取消と切り分けて維持。
  実際にcommitが完了した後の停止は、そのcommitを巻き戻したことにしない。
  ブラウザ再起動後や複数タブ全体の停止保証は、この同一runtimeの修正とは別。
- **検証**：`user-cache-write-lifecycle.spec.ts`を追加し、v8に対して親が実行。
  native IndexedDBの未確定／complete境界で停止させた両ケースがRED。
  未確定時は旧本文、complete時は新本文が必要だが、両方とも再起動後に本文が読めなかった。
  実行コマンド：
  `node apps/web/scripts/check-offline-candidate.mjs review-artifacts/user-cache-suspension-v8 browser user-cache-write-lifecycle.spec.ts`
  （exit 1、2 failed）。後継候補の既定検証は既存27件＋この2件の計29件。
- **見直し元**：[v8レビュー結果](../review-artifacts/user-cache-suspension-v8/parent-review.md)。

## D23：エージェントのチェックポイントを署名なしで保存

- **状態**：ユーザー承認により選択、今後のcommitへ適用。
- **選択前チェックポイント**：`03c4d37`。
- **背景**：ユーザー不在時に署名付きcommitが失敗するため、
  署名を外してcommitしてよいと明示された。
- **選択肢**：A＝各commitに`--no-gpg-sign`を指定、
  B＝リポジトリまたはユーザー全体の署名設定を無効化。
- **採用**：A。エージェントの操作だけに限定し、ユーザーの通常の署名設定を維持する。
- **制約**：既存commitの署名や履歴は書き換えない。pushや公開の追加許可ではない。
- **実行ルール**：`GIT_EDITOR=true git commit --no-gpg-sign ...`を使い、
  Gitの書き込み操作を並列・重複実行しない。
- **実行上の記録**：直前のチェックポイント作成では親がcommitコマンドを
  重複送信し、一方がindex.lock競合で失敗した。成功済み`03c4d37`と
  cleanな作業ツリーを読み取り専用コマンドで確認し、ロックファイルは削除していない。

## D24：個別テストの候補読み込み検証

- **状態**：修正・検証済み。
- **選択前チェックポイント**：`b2cd66c`。
- **背景**：保存層だけを使う個別テストでも、ランナーが
  `note-read-session.ts` の読み込みを要求して終了コード1になっていた。
- **選択肢**：A＝既定の全件検証は両main module必須、指定された個別検証は
  少なくとも1候補moduleの読み込みを必須とする。
  B＝読み込みチェックを全部削除。C＝個別テストにも不要なmoduleをimportさせる。
- **採用**：A。テストの依存関係を歪めず、候補を一切実行していない検証の
  誤った成功扱いを防げる。
- **検証**：v8の `offline-cache.spec.ts` 単独は3件成功・exit0。
  候補moduleを使わない `storage-platform.spec.ts` 単独はブラウザ自体が成功しても、
  ランナーが「候補未読込」でexit1となることを確認。ランナーのBiomeも成功。

## D25：v9の実コードと共有されたファイルの不一致

- **状態**：未採用、既存のテスト済みbytesを回収中。
- **背景**：v9は29件成功の報告があったが、親が受信した保存層ファイルは
  2行のplaceholder、判断・検証ファイルは見出しだけだった。
  これらをテスト済み候補として扱わない。
- **確認した事実**：サブエージェントは最初のplaceholderをapply_patchで作り、
  その後terminalのcpで本文を配置したと報告。子側ディスクには完全な本文がある一方、
  親には最初のplaceholderだけが共有された。ソースを実装用ファイルへ反映してはいない。
- **選択肢**：A＝既存の完全なbytesを編集ツールの引数に直接渡し、分割Add/Updateで
  再公開してから同一ハッシュ・再検証を確認する。B＝同じterminalコピーを繰り返す。
  C＝テスト済みコードを諦めて新規再実装する。
- **採用**：A。原典が残っている限り、再実装による別コードへの置き換えを避ける。
  全文を1回で送れなければ100～150行単位の編集、または全文を分割メッセージで回収する。
- **追加確認**：子から提示されたGit blob IDは親のobject databaseには存在しなかった。
  clone間のGit object共有を前提にしない。単なる先頭コメントの変更でも本文の
  共有が保証されるとは扱わない。
- **検証条件**：親で全文とハッシュを確認し、候補ランナーを再実行するまで採用しない。
  この受け渡し修復は実装方針D22を変えるものではない。
- **回収の進展**：子の報告した絶対パスを、実装の依存資料として親が読み取り専用で確認。
  同一マシンの子worktreeに完全な本文があり、報告されたハッシュと一致した。
  原典の余計な再実装を避け、新しいコンテキストのサブエージェントへ、完全bytesを
  小分けのapply_patch引数で再公開する作業のみを依頼した。
- **原典ハッシュ**：保存層`515a9c378f86e65e6def3427074c32a423743d0058ffd1099ae8047637958fcd`、
  セッション`081bb2dbd02aedecf096b7f6cec7cfde13979a64e0cc2c00c23c1c363da260b7`。
  初回報告後の先頭コメント追記を含むため、回収後はこの内容を改めて検証する。
- **結果**：親が分割回収の残りを補完し、両ファイルの完全SHA256一致を確認。
  回収後に型チェック・Biome成功、既存29ブラウザ成功、D26の追加1件のみREDを確認した。
  判断・検証報告も `review-artifacts/user-cache-suspension-v9-recovered/` に保存した。

## D26：DB接続失敗時にもノート拒否の順序を確定する

- **状態**：推奨案を選択。親の回帰テストRED、回収後の候補で修正する。
- **選択前チェックポイント**：`c28bd37`。既存v9のbytes回収は修正せず別作業として完了させる。
- **背景**：v9原典のセッションは403後にDBを開けない場合、ユーザーのキャッシュを
  停止するだけでノート拒否の世代を進めない。先行する遅い200応答はキャッシュ保存に
  失敗しても成功結果として返る。R02・R03で合意した拒否入口の独立性が不足していた。
- **選択肢A**：拒否を受けた時点で、ストレージを待たずユーザー・ノートの順序を更新。
  永続化・cleanupはその後に行い、同じ拒否を二重に別の拒否として扱わない。
- **選択肢B**：ユーザーのキャッシュ停止時に、そのユーザーのネットワーク表示も
  一律禁止する。単純だが、正しい新しいオンライン応答まで表示できなくなる。
- **採用**：A。古い要求は排除し、キャッシュ利用停止と新しい正常なオンライン表示を分離する。
- **検証**：親が `note-denial-entry-ordering.spec.ts` を作成し、子worktreeの原典bytesを
  Viteへ読み取り専用で注入して実行。403と警告は返るが、先行する200が成功を返すため
  `oldPublished=false` の期待に対しtrueとなりRED（exit1、1 failed）。
  同じテストは拒否後の新しい200によるオンライン表示を維持することも要求する。
- **次の検証対象**：候補ランナーの既定対象を29件から30件へ追加。
  回収専任の作業は依頼時の29件で原典を保存し、この既知のREDを後継修正へ引き継ぐ。
- **後継結果**：同期的な`enterOfflineNoteDenial`でDB接続前に順序を確定する候補が届いた。
  親で実コードと報告ハッシュの一致を確認。担当は30件成功を報告したが、
  直接のキャッシュAPI呼び出しで更新が抜けていたためD28へ補修する。まだライブへ未採用。

## D27：候補の正本を固定し、版はGitで保存する

- **状態**：既存ディレクトリを正本として選択。コードのbytesは変更しない。
- **選択前チェックポイント**：`267250b`。
- **背景**：各候補版で新しいディレクトリへ全文を複製することが、
  受け渡し失敗と大量の重複ファイルを生んでいた。現在は完全な候補を回収・コミット済み。
- **選択肢**：A＝`review-artifacts/user-cache-suspension-v9-recovered/`を正本として直接編集し、版をcommitで保存。
  B＝今後もv10、v11などのディレクトリへ全文コピーを繰り返す。
- **採用**：A。以後のサブエージェントは既に共有されたファイルへの小さなapply_patchだけで
  作業でき、原典のコピーやライブソースの復元を不要にする。
- **制約**：実装用`apps/web/src/`への採用とは別。過去の候補・判断・検証はGitから辿れるよう残す。
  全文コピーや履歴の書き換えで過去版を作り直さない。
- **確認**：既存2ファイルのSHA256はD25の回収済み原典と同一。
- **実行上の見直し**：当初は短い新ディレクトリ名へapply_patchのMoveを試みたが、
  後続のファイル・端末確認で移動先は存在せず、元ファイルが保持されていた。
  名前変更を進捗の前提にせず、存在・内容・Git保存を確認できた既存パスを正本とする。

## D28：直接の拒否APIでも同じ順序ルールを使う

- **状態**：修正・親レビュー・親の31件再検証完了。D29でライブ基盤へ反映する。
- **選択前チェックポイント**：`2a57878`。D26修正候補と本テストを補修前に保存する。
- **背景**：二重更新を避けるD26修正で、`cache.denyNote(id)`を直接呼ぶ場合の
  世代更新もなくなった。既に本文を読み取っている `getNote` が、拒否完了後に古い本文を返す。
- **選択肢A**：順序トークンが明示されていない直接呼び出しは、共通の同期拒否入口で
  新しい拒否を確定する。明示されたトークンは再利用し、二重に世代を進めない。
- **選択肢B**：直接APIを廃止し、全呼び出し元へ事前の世代更新手順を要求する。
  誤用の余地と呼び出し側の知識を増やすため、この小さな修正では選ばない。
- **採用**：A。公開キャッシュAPIとして一貫した振る舞いを保ち、既存の共通入口を再利用する。
- **検証**：親作成 `offline-direct-denial.spec.ts` はOPFS本文読み取りを保留し、
  直接の拒否完了後に再開する。対象候補で本文が返ったためnull期待に失敗
  （exit1、1 failed）。既定テストは31件へ追加した。
- **結果**：トークン未指定時のみ`enterOfflineNoteDenial`を呼び、指定済みは再利用する
  小さな差分を採用。親が型チェック・Biome・31ブラウザを再実行して成功。
  詳細ハッシュは固定候補の`validation.md`へ記録。

## D29：レビュー済み候補のライブ基盤への反映

- **状態**：ライブ基盤へ反映し、親による再検証完了。
- **選択前チェックポイント**：`b6794b9`。D28後の候補と親検証も反映前に保存する。
- **選択肢A**：既存のtrackedソースへ、レビュー済み候補との差分をapply_patchで適用する。
  B＝候補を移動してライブソースを置き換える。C＝terminalのcpで上書きする。
- **採用**：A。既存パスを保ち、これまで問題があったMove・terminal上書きを使わない。
- **許容する差異**：候補専用の先頭コメントのみ除く。それ以外のコードbytesを候補と照合する。
  新しい振る舞いを反映作業へ混ぜない。
- **検証**：候補専用ランナーではなく通常のライブソースを読む31ブラウザ、
  Web unit・型チェック・Biomeで確認する。
- **範囲**：保存・読み取り基盤のみ。実画面接続、PWA起動、prefetch、画像、
  ログアウト・複数タブ・本番配信の検証は後続であり、完成扱いにしない。
- **結果**：親が候補先頭コメントを除く全bytes一致を確認。
  通常のライブソースを読むブラウザ31件、Web unit117件、型チェック、
  Web本番ビルド、対象2ファイルのBiome、diff-checkがすべて成功した。
- **ビルド上の注意**：500kBを超えるchunkの警告は出るが、ビルドは成功。
  この基盤採用にコード分割の変更は混ぜない。
- **採用ソースSHA256**：保存層`f8c004b8d3ce250f23571b44efc136e1f36efe006b66ed42122e3fef861648d3`、
  セッション`e6d112c9af925ca9a77f2aa6c78cc55471f43aafc3243f5d9a34bb7cfa582830`。

## D30：実画面接続の前に未確認viewerの読み取りを分離する

- **状態**：候補レビュー・親再検証後、ライブの読み取り基盤へ反映済み。
- **選択前チェックポイント**：`b4601b3`（レビュー済みキャッシュ候補の採用前保存）。
- **背景**：調査でAppShell・Editorがまだ旧Map／SSR読込を使い、更新gateや
  collaborationに実際のデータ取得元が接続されていないことを確認した。
  前回のcached viewer IDを認証済みとしてネットワーク結果の保存先に使ってはならない。
- **選択肢A**：未確認のcached viewerではローカル読込に限定し、認証済み／確認済みguestの
  ネットワーク読込とは先に分離する。その後、実画面へセッション結果を接続する。
- **選択肢B**：先に従来のネットワーク優先読込を全viewerへ接続し、UIだけreadonlyにする。
  未確認IDへの保存を許す余地が残るため選ばない。
- **採用**：A。UIに接続する前に、認証とキャッシュ所有者の境界をpublic APIテストで固定する。
- **制約**：キャッシュ未取得を偽のHTTP404や認証済み状態へ変換しない。
  実画面の更新dispatch方式は、明示的facadeと共通API入口を比較したうえで別途記録する。
- **調査記録**：[ノート画面への接続](offline-note-view-integration.md) に参照先、
  既存の受け入れテスト、SSR・共同編集・更新経路の注意点を保存した。
- **開始時の検証**：`cached-viewer-note-read.spec.ts`を追加し、採用済みライブ基盤でREDを確認。
  cached viewerでもnetwork由来の本文を受け取り、cachedAtもnullとなっていた。
  このテストはローカル本文・取得日時の保持、キャッシュミス時もネットワーク要求0件、
  前回ユーザーの本文を上書きしないことを要求する。
- **結果**：既存ローカル取得と共通の最終返却チェックを再利用し、cached viewerでは
  fetchを行わない分岐を採用。親が候補32件とライブ32件、unit117件、型チェック・Biomeを確認。

## D31：ローカルだけの読み取りでキャッシュがない場合

- **状態**：D30とともにライブ採用・検証済み。
- **選択前チェックポイント**：`ee242f2`（ライブ基盤、31ブラウザ・117unit・build成功）。
- **選択肢A**：`OfflineNoteUnavailableError`という公開エラー型で識別する。
  B＝HTTP404の失敗結果を合成する。C＝既存read-result unionへ新しい失敗variantを追加する。
- **採用**：A。既存の読み取りは通信不能や中止を例外として返すため、同じ方式で
  ローカル未取得を識別できる。HTTP応答がないのに404を作るBは不適切。
  Cは将来有効だが、この小さなスライスでは呼び出し側の判別条件を増やさない。
- **契約**：cached viewerのローカル未取得・利用不能を、この型で返す。
  ネットワークへ取りに行かず、既存データを変更せず、HTTP statusを付与しない。
  中止理由をこのエラーへ変換せず、セッションの最終有効性判定を維持する。
- **範囲**：認証済み／確認済みguestのネットワーク優先経路は変更しない。
  画面接続や更新dispatchは次のスライス。
- **採用ソースSHA256**：`note-read-session.ts`
  `b49748b4b9c42b11fc1b5ced350f6c91e21545e7515d12b6d7036d43c95e4dbb`。

## D32：AppShellがviewer contextを所有する

- **状態**：D34補修後に親レビュー・再検証を終え、ライブへ反映済み。
- **選択前チェックポイント**：`2df0b81`（cached viewerのローカル読み取りを採用済み）。
- **選択肢A**：AppShellが`resolveViewerContext`を使ってviewerを所有し、
  既存のuserはそこから導出する。auth-configは独立に解決する。
  B＝各ページがviewerを別々に解決する。C＝userとviewerを別々の可変stateとして持つ。
- **採用**：A。認証の取得を重複させず、cached viewerを認証済みにしない共通状態を作る。
  ノートのsourceまでAppShellが推測することはせず、ページの実際の読込結果に任せる。
- **互換性**：既存のuser/userLoading/setUser/setHeaderを維持し、contextにviewerを追加。
  ProfileSettingsのsetUser更新は同じユーザーのキャッシュ所属だけを維持し、
  未確認の別IDへの保存先を作らない。古いbootstrapが後から上書きしないよう中止する。
- **失敗時**：auth-config失敗はviewer成功を失わせない。viewerのcached復元判断は
  既存resolverへ集約し、他のbootstrap例外から勝手にcachedへ変換しない。
- **検証**：実AppShellと公開Outlet contextを観測するStrictMode fixtureを追加。
  auth-config通信失敗＋me成功で認証済みviewerを要求し、次のreloadではmeも通信失敗させて
  user:nullのcached viewerを要求する。現状はuser:null/viewer:nullでRED。
- **範囲**：ノート画面、更新dispatch、logout purgeはこの小スライスには混ぜない。
- **結果**：親が候補34件を再検証し、2つのライブファイルが候補と完全一致することを確認。
  ライブブラウザ34件、Web unit117件、型チェック、Webビルド、Biome・diff-checkが成功。

## D33：UI候補の階層を候補ランナーで扱う

- **状態**：拡張・基本検証済み。
- **背景**：従来のランナーは直下の `.ts` を `src/lib/` として扱うだけだった。
  AppShellの候補をライブへ先に入れず、元と同じ相対importで検証したい。
- **選択肢A**：候補内の `src/**` の `.ts` / `.tsx` を同じ実装パスへoverlayする。
  B＝候補だけimportを書き換える。C＝UIだけライブへ先に配置して検証する。
- **採用**：A。候補と採用後のコードを同じに保ち、レビュー前のライブ反映を避ける。
  既存の直下 `.ts` 方式は互換維持し、同じ実装パスへの重複候補は拒否する。
- **検証**：既存候補の型チェックとローカル読込テスト1件は成功。
  階層付きAppShell候補の型チェック成功と、その候補にだけ追加した型エラーの検出を確認。
  実AppShell fixtureのREDは候補UI実装後にGREENへ進める。
- **制約**：実装用srcは変更しない。型チェックは検証専用ツリー、ブラウザはVite overlay、
  検証前後のファイル不変チェックを維持する。

## D34：auth-configもeffectの寿命に従わせる

- **状態**：effect-localなactive/cleanupで修正、D32とともにライブ採用・検証済み。
- **選択前チェックポイント**：`04db55b`。D32候補はまだライブへ未採用。
- **背景**：viewer側にはStrictModeでの取消・古い結果の排除があるが、
  config側は無条件の`.then(setAuthConfig)`で、終了したeffectの応答が後の設定を上書きする。
- **選択肢A**：config専用effectのactiveフラグとcleanupで、終了後の結果を無視する。
  B＝viewerと同じリクエスト世代へ結合する。C＝全fetchに新しい取消APIを追加する。
- **採用**：A。configはコンポーネント寿命で管理し、プロフィール更新による
  viewerだけの中止に巻き込まない。小さな変更で既存の独立性を維持できる。
- **検証**：StrictModeの最初のconfig応答を保留し、後の設定でログインリンクを表示した後、
  古いmock-login設定を返す。現在の候補ではメール入力が現れ、期待0件に対し1件となりRED。
- **テスト準備の修正**：メニューのroleはlinkでなくmenuitem。またメニューを開くために
  実アプリ同様のThemeProviderが必要だった。これらの不足による先行失敗は製品の回帰証拠と
  扱わず、fixture修正後に上記の本来のREDを確認した。fetchは外部境界で制御し、React内部はmockしない。
- **記録保全**：D32担当がrootの判断・検証記録を置換したため、元の全文を戻し、
  D32は別Markdownへ保存してリンクを追記する形で修復した。UIコードのhashは不変。
- **採用ソースSHA256**：AppShell
  `35e12ccf2834601f960cce89e9ee4e918b630298391883ce23d1f02e5e8d25da`、
  AppShellContext `0ff5b9d61b118865cde5859b5557eab56c21e76eeafdf2d3d680e47d78de2915`。

## D35：候補サーバーへ実際の空きポート番号を渡す

- **状態**：修正・検証済み。
- **背景**：親が検証コマンドを重複実行した際、Viteがport0を既定5173として扱い競合した。
  重複実行は避ける。それとは別に、ランナーの「空きポート」保証も正しく実装する必要がある。
- **選択肢A**：Nodeの一時サーバーで空き番号を取得し、Viteへ正の番号とstrictPortを渡す。
  B＝Viteの内部HTTPサーバーを直接操作する。C＝5173等の固定番号を使い続ける。
- **採用**：A。公開APIで制御し、他サーバーを再利用・停止しない。
  選択から起動までに別プロセスが番号を取得したら、strictPortにより失敗させる。
- **確認方針**：Vite起動後の実ポートが選択した番号と一致することをランナー自身が検査。
  既知の単独候補テストで動作確認し、ポート競合で失敗した重複実行をコード不具合の証拠に数えない。
- **結果**：実ポート一致チェックを含む候補のローカル読込テストが1件成功し、
  ランナーとfixture/testのBiome・diff-checkも成功した。

## D36：更新dispatchへ所有権付き閲覧スコープを渡す

- **状態**：共通policyとしてライブ採用済み。API・画面への接続は後続。
- **選択前チェックポイント**：`d755e26`（AppShell viewer contextのライブ採用）。
- **背景**：更新APIが現在のデータのcache/network由来を参照する必要がある。
  古い画面の遅い結果やcleanupが、新しい画面・viewerの許可状態を変更してはいけない。
- **選択肢A**：AppShellのviewer getterと、現在の閲覧スコープの明示的な結果を
  共通dispatchの判定へ渡す。B＝各更新APIへ独立のreadonlyフラグを追加。
  C＝認証済みかどうかだけで更新を許可する。
- **採用**：A。既存の`createMutationGate`のルールを再利用し、
  出所の推測やフラグの付け忘れを避ける。キャッシュの保存先決定には使わない。
- **公開契約**：`createViewingAccess(readViewer)`は`getAccess`と`beginView(ownerViewer)`を返す。
  スコープは`publish(access): boolean`と`dispose()`を持つ。新しいスコープはpendingから始める。
  古い／別viewerのスコープは新しい状態を置換できず、古いdisposeも現在のスコープを消せない。
- **データ境界**：閲覧結果のviewer/sourceをスナップショットとして保持し、
  呼び出し側が渡した値や取得した値を書き換えても権限状態を変更できないようにする。
  読み取り結果のviewerは複製されるため、publish時は所有者のidentity/mode/cache所属を照合する。
- **binding契約**：`bindMutationAccess(getAccess)`は解除関数を返し、
  古い解除関数が新しいbindingを消さない。`runMutation(operation)`は最新bindingから
  既存gateで判定し、binding不在ならReadOnlyViewingErrorで実行を拒否する。
- **スコープ外**：閲覧スコープがない一般画面では、確認済みviewerのnetwork操作を許可する。
  cached/unavailable viewerは既存gateにより拒否される。サーバーの権限検証の代用ではない。
- **検証**：`viewing-access.spec.ts`を作成し、新モジュールが存在しないためREDを確認。
  pending、cache、viewer変更、古いpublish/dispose、入力値変更、古いbinding解除をpublic APIで検証する。
- **範囲**：このスライスはpolicyモジュールのみ。API・AppShell・Editorへの接続は後続。
- **結果**：親が候補35件・型チェック・Biomeを再確認。ライブへ同一bytesを配置し、
  policyの公開APIテスト1件、Web unit117件、型チェック・Biomeを確認した。
  採用SHA256は`439b32f697a82f9fdbb5ee7f1d3953b17ca8d632d9a6e7d307950363388b5214`。

## D37：既存Editorの状態を維持して読み取り専用へ接続する

- **状態**：実装方向を選択、D36後に画面・API接続のテストへ進む。
- **選択肢A**：既存Editorの読込をセッションへ接続し、同じ結果からpreview・編集可否・
  collaboration・更新dispatchを制御する。B＝別のreadonlyページを作り、Editorと切り替える。
- **採用**：A。既存のMarkdown previewと編集状態を再利用し、切断時の画面切替で
  入力済みdraftを捨てる構造を避ける。処理の見通しが悪ければ小さなhookへ分ける。
- **注意**：最初の主要対象は既存の`/n/:id`。`/s/:id`は実際にはSharePageへ向かう別経路で、
  Editorへ勝手に変更しない。短縮ID・別名の扱いは仕様の確認項目に沿って後続で検証する。
- **後続テスト**：既存offline-note-viewに加え、me成功＋ノートだけ503でも
  cache由来としてreadonlyになることを確認し、authだけで許可する回帰を防ぐ。

## 今後の記録テンプレート

新しい判断を行った時点で、次を追記する。失敗しても記録を消さない。

```markdown
## Dxx：判断名
- 状態：提案／選択済み／実装中／検証済み／棄却／後継へ変更
- 選択前チェックポイント：
- 背景・観測した問題：
- 検討した選択肢と各案の利点・欠点：
- 推奨・採用した案と理由：
- 影響範囲・制約・未確認事項：
- テスト／再現手順と実際の結果（実装・候補のハッシュも区別）：
- 実装／検証コミット：
- 見直した場合の原因・後継判断：
```

## 完了時に整理する項目

- 利用者が実際にできることと対象外。
- 最終採用した保存・取得・拒否・停止・容量・更新方針。
- 棄却した案と、それを棄却できたテスト・レビューの証拠。
- 実ブラウザのオフライン再起動、フォルダ探索、本文表示、本番配信の結果。
- 容量不足・中断・認証変更・複数タブ・アップデートに残る制約。
- 決定台帳、主要実装、復帰可能なコミットへの参照。
